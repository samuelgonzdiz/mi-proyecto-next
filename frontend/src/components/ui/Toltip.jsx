/* 'use client';
import { cn } from "@/lib/cn"

export default function Toltip({ children, className }) {
    return (
        <div className={cn(
            "absolute right-full top-1/2 -translate-y-1/2 mr-2 opacity-0 group-hover/button_toltip:opacity-100 transition bg-black text-white text-xs px-2 py-1 rounded whitespace-nowrap shadow-xl",
            className
        )}>
            {children}
            <div className="absolute left-full top-1/2 -translate-y-1/2 w-0 h-0 border-t-4 border-b-4 border-l-4 border-t-transparent border-b-transparent border-l-black"
            ></div>
        </div>
    )
} */
